import EntryPage from '../app/entry/[token]/page';
import EntryForm from '../app/entry/[token]/entry-form';

describe('EntryPage', () => {
    it('should be defined', () => {
        expect(EntryPage).toBeDefined();
    });
});

describe('EntryForm', () => {
    it('should be defined', () => {
        expect(EntryForm).toBeDefined();
    });
});
