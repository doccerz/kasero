Phase 1 COMPLETE: Created migration 0007_profile_void_billing.sql; updated schema.ts — added 'voided' to contractStatusEnum, billingDateRule to contracts, billingDate to payables, name/email to adminUsers.
Phase 2 COMPLETE: Created profile.service.ts (getProfile, updateProfile, updatePassword with bcrypt), profile.controller.ts (GET/PATCH /admin/profile, PATCH /admin/profile/password with JwtAuthGuard), profile.module.ts; added ProfileModule to app.module.ts; wrote 10 unit tests (service + controller specs), all passing.
Phase 3 COMPLETE: Added ContractsService.void() — fetches contract, rejects non-posted with 400, runs transaction to set status='voided' and insert audit row; added POST :id/void endpoint (HTTP 200); updated LedgersService.recordPayment() to fetch contract first and reject voided contracts with 400; updated recordPayment unit tests to use new contract-aware mock builder; wrote 6 new tests (3 service, 2 controller, 1 ledger), all passing.
Phase 4 COMPLETE: Added billingDateRule? to GeneratePayablesParams and generatePayables() — each row now includes billingDate (clamped per month, defaults to dueDate when billingDateRule absent); ContractsService.post() passes existing.billingDateRule; LedgersService.getLedger and DashboardService.getDashboard now use (billingDate ?? dueDate) for totalOwed/pastDuePayables filters; wrote 7 failing tests (3 generatePayables, 2 ledger, 2 dashboard), all now passing; full test suite pre-existing public-access failures are unrelated to Phase 4.
