Step 1 DONE — wrote failing unit tests for SettingsService in apps/api/src/settings/settings.service.spec.ts (TDD: tests cover onApplicationBootstrap cache load, get, getBoolean, and a DB-backed bootstrap test). Tests are intentionally failing — implementation pending.
Step 2 DONE — implemented SettingsService (cache via Map, onApplicationBootstrap loads all settings rows, get/getBoolean helpers). Added exports: [SettingsService] to SettingsModule. All 5 unit tests pass; 1 DB-backed test self-skips without DATABASE_URL.
Step 3 DONE — wrote failing unit tests for TenantsService in apps/api/src/tenants/tenants.service.spec.ts (TDD: tests cover findAll with hide_expired false/true, null expiry inclusion, and 2 DB-backed tests). Tests were intentionally failing — implementation pending.
Step 4 DONE — implemented TenantsService.findAll() with SettingsService.getBoolean('tenant.hide_expired') filter (or/isNull/gt/sql from drizzle-orm). Added SettingsModule to TenantsModule imports. All 3 unit tests pass; 2 DB-backed tests self-skip without DATABASE_URL. Mock pattern used a thenable query object to support both direct-return and .where() chain.
Step 5 DONE — extended settings.module.spec.ts to verify SettingsService is exported and injectable from SettingsModule. All tests pass (19 suites, 51 passing, 37 skipping).
